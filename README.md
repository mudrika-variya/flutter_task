# Mudrika Variya Flutter Task (Tournament App)

small demo app for Tournament App task. login with dummy auth API 

- login screen, use below credentials:
  ```dart
  username: 'emilys',
  password: 'emilyspass'
  ```
- data comes from a json file, no real backend.
- user get ₹500 in wallet at login, join minus fee, can join the tournaments with the wallet amount.
- leaderboard has 20+ players and scrolls lazy.
- used proper clean architecture and GetX for state management, with validation and error handling.

Thanks.
